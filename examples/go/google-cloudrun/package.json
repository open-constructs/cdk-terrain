{
  "//": "Disabled via the 'private' attribute: main.go reads cdktf-example.json at synth time for GCP credentials, which CI does not have.",
  "name": "@examples/go-google-cloudrun",
  "private": true,
  "version": "0.0.0",
  "license": "MPL-2.0",
  "scripts": {
    "reinstall": "rm go.sum",
    "build": "cdktn get",
    "beforeSynth": "go mod tidy",
    "synth": "cdktn synth"
  }
}
