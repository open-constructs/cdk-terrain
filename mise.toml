[settings]
idiomatic_version_file_enable_tools = ["node"]

[tools]
python = '3.11'
go = '1.25'
java = "prefix:corretto-20"
dotnet = "6.0"  # Match jsii-superchain DOTNET_CHANNEL
uv = "0.11.19" # Used by the pipx backend
"pipx:pipenv" = "latest"

terraform = "1.7.5"
"github:specledger/specledger" = "1.0.56"

# # Works on Linux (not MacOS)
# "ubi:getsentry/sentry-cli" = "3.1.0"
